let users: string[] = ["ali"];
let randoms: (string | number | boolean)[] = [12, "amin", false];
let roles: (number | string)[][] = [
    [1, "USER"],
    [2, "ADMIN"],
];

users.push("admin");

console.log(randoms);
console.log(roles);
